# Copy to .env or set these values in your CI secret store.
# TEST_ENV can be dev or test. Defaults to dev.
TEST_ENV=dev

# Optional overrides. When omitted, values from config/environment.ts are used.
BASE_URL=https://dev.example.com
LOGIN_USERNAME=standard.user
LOGIN_PASSWORD=change-me
